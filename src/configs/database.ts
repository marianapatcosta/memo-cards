const DATABASE_NAME = '@memo-cards'

// @app:collection
const COLLECTION_CARDS = `${DATABASE_NAME}:cards`
const COLLECTION_LOCALE = `${DATABASE_NAME}:locale`
const COLLECTION_THEME = `${DATABASE_NAME}:theme`

export {
  COLLECTION_CARDS,
  COLLECTION_LOCALE,
  COLLECTION_THEME,
}
